import { MySampleDirectiveDirective } from './my-sample-directive.directive';

describe('MySampleDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new MySampleDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
