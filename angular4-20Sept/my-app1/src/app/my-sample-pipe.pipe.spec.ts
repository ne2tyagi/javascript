import { MySamplePipePipe } from './my-sample-pipe.pipe';

describe('MySamplePipePipe', () => {
  it('create an instance', () => {
    const pipe = new MySamplePipePipe();
    expect(pipe).toBeTruthy();
  });
});
